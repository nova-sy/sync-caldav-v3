# CalDAV 同步工具配置文件示例
# 复制此文件为 .env 并填入你的实际配置信息

# ==========================================
# 全局设置
# ==========================================
OUTPUT_DIR=calendar_events
TIMEOUT=30

# ICS 文件自定义名称（可选）
# 如果设置，最终生成的文件将命名为 all_calendars_[ICS_FILE_NAME].ics
# 如果不设置，将使用时间戳命名 all_calendars_YYYYMMDD_HHMMSS.ics
ICS_FILE_NAME=latest

# ==========================================
# 钉钉账号配置
# ==========================================
# 钉钉账号显示名称
DINGTALK_ACCOUNT_NAME=钉钉日历账号

# 钉钉用户名（通常是邮箱或手机号）
DINGTALK_USERNAME=your_dingtalk_username

# 钉钉密码（建议使用应用专用密码）
DINGTALK_PASSWORD=your_dingtalk_password

# 钉钉 CalDAV 服务器地址
# {username} 会被自动替换为实际用户名
DINGTALK_URL=https://calendar.dingtalk.com/dav/{username}/

# 钉钉同步时间范围（可选，单位：天）
DINGTALK_SYNC_DAYS_PAST=90
DINGTALK_SYNC_DAYS_FUTURE=90

# ==========================================
# 腾讯会议账号配置
# ==========================================
# 腾讯会议账号显示名称
TENCENT_ACCOUNT_NAME=腾讯会议账号

# 腾讯会议用户名（通常是邮箱）
TENCENT_USERNAME=your_tencent_username

# 腾讯会议密码（建议使用应用专用密码）
TENCENT_PASSWORD=your_tencent_password

# 腾讯会议 CalDAV 服务器地址
# {username} 会被自动替换为实际用户名
TENCENT_URL=https://cal.meeting.tencent.com/caldav/{username}/calendar/

# 腾讯会议同步时间范围（可选，单位：天）
TENCENT_SYNC_DAYS_PAST=90
TENCENT_SYNC_DAYS_FUTURE=90

# ==========================================
# 可选配置（如需要其他服务可取消注释并配置）
# ==========================================

# Google 日历配置（暂未实现）
# GOOGLE_ACCOUNT_NAME=Google 日历账号
# GOOGLE_USERNAME=your_google_username
# GOOGLE_PASSWORD=your_google_app_password
# GOOGLE_URL=https://apidata.googleusercontent.com/caldav/v2/{username}/events/

# Outlook 日历配置（暂未实现）
# OUTLOOK_ACCOUNT_NAME=Outlook 日历账号
# OUTLOOK_USERNAME=your_outlook_username
# OUTLOOK_PASSWORD=your_outlook_password
# OUTLOOK_URL=https://outlook.live.com/owa/calendar/{username}/

# ==========================================
# 配置说明
# ==========================================
#
# 1. 用户名格式：
#    - 钉钉：通常是邮箱地址或手机号
#    - 腾讯会议：通常是邮箱地址
#
# 2. 密码安全：
#    - 建议使用应用专用密码而非主账号密码
#    - 钉钉：在安全设置中生成应用密码
#    - 腾讯会议：在账号设置中生成应用密码
#
# 3. URL 格式：
#    - {username} 占位符会被自动替换
#    - 确保 URL 末尾的斜杠正确
#
# 4. ICS_FILE_NAME 说明：
#    - 设置为 "latest" 将生成 all_calendars_latest.ics
#    - 设置为 "my_calendar" 将生成 all_calendars_my_calendar.ics
#    - 留空或不设置将使用时间戳命名
#
# 5. GitHub Actions 配置：
#    - 本地开发使用此 .env 文件
#    - GitHub Actions 使用 Repository Secrets 或 Environment Secrets
#    - 不要将 .env 文件提交到版本控制系统
#
# 6. 测试配置：
#    - 配置完成后运行：python main.py --list
#    - 检查账号是否正确识别
#    - 运行同步测试：python main.py --sync-all
